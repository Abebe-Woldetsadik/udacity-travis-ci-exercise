version: 2.1
jobs:
  build:
    environment:
      IMAGE_NAME: simple_node
    docker:
      - image: cimg/base:2022.05
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker image
          command: docker build -t abebewoldetsadik/simple_node:latest .
          
  publish-latest:
    environment:
      IMAGE_NAME: simple_node
    docker:
      - image: abebewoldetsadik/simple_node:latest
    steps:
      - setup_remote_docker
      - run:
          name: Publish Docker Image to Docker Hub
          command: |
            echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
            sudo docker push abebewoldetsadik/simple_node:latest
workflows:
  version: 2.1
  build-master:
    jobs:
      - build:
          filters:
            branches:
              only: main
      - publish-latest:
          requires:
            - build
          filters:
            branches:
              only: main
